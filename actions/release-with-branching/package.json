{
  "name": "release-with-branching",
  "private": true,
  "version": "1.0.0",
  "description": "A GitHub Action to publish packages to npm and create maintenance branches for multi-release workflows.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "node --test --import '../../test/setup.js' ./**/*.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@systemcraft-stack-actions/utils": "workspace:*"
  }
}